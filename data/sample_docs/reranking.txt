Reranking is a technique used in advanced RAG pipelines to improve retrieval quality. After initial retrieval a reranker model scores each retrieved document for relevance to the query. Cross-encoder models like BGE-Reranker provide more accurate relevance scores than bi-encoder embeddings. Context compression further reduces noise by extracting only the most relevant parts of each document. Together reranking and compression significantly improve answer quality.