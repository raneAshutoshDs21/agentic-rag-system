LangGraph is a library for building stateful multi-actor applications with LLMs. It extends LangChain with graph-based orchestration. In LangGraph, nodes represent processing steps and edges define transitions. It supports cycles, branching, and conditional logic. LangGraph enables building complex agentic workflows with human-in-the-loop capabilities. State is persisted across nodes using a shared state object.